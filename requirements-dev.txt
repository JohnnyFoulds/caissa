# requirements-dev.txt — development and testing dependencies
#
# These are not required to run Caissa. Install alongside requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt
#
# For CV/OCR tests also install:
#   pip install -r requirements-rpa.txt

# Testing
pytest>=8.0
pytest-timeout>=2.3
pytest-cov>=5.0

# Linting
ruff>=0.4

# Documentation
sphinx>=7.0
sphinx-rtd-theme>=2.0
